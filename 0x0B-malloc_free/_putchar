#include "main.h"
#include <unistd.h>

/**
*_putchar - writes the character c to stdout
*@c: The ccharacter to print
*
*Return:on sucsess 1.
*on error, -1is returned, and errno is set appropiatley.
*/

int_putchar(char c)
{
return (write(1,&c,1));
}
